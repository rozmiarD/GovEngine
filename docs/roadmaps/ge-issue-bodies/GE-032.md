Task: GE-032 — Implement optional constrained local runner only if readiness passes

Context:
Signposter status:

* Planner draft / validate / seed / next / mark surfaces exist locally.
* Current planner mode is supervised and local-first.
* GitHub mutation is forbidden unless explicitly guarded by --apply.
* OpenClaw execution is forbidden unless explicitly guarded by --execute.
* Source plan goal: GE - GovEngine governed-runtime kernel MVP: compose runtime admission, trust, receipt/evidence, audit/replay, runner safety, inspect-only workflow, docs, validation, and Signposter lifecycle without live execution by default.

Problem:
Purpose: Add a disabled-by-default LocalSubprocessRunner only when all safety prerequisites are present.
Concrete scope: if GE-031 says ready, implement argv-only, cwd/env allowlists, timeout, max output, output digests, redaction hook, explicit live flag, and bounded receipt; otherwise deliver not-applicable evidence artifact only.
Non-goals: no broad rewrite, no live backend enablement, no PKI/KMS ownership, no Ravenclaw/OpenClaw/MCP/A2A core dependency, no package publish.
Route/model: role:worker through Signposter lifecycle; expected tier: standard coding worker; use stronger reasoning for governance ambiguity.
Gate: gate:review.
Risk level: high.
Mutation boundary: dry-run first; GitHub mutation only through guarded Signposter --apply paths; no upstream mutation; no package publish; no tag creation.
Done criteria: scoped implementation or audit artifact is complete, bounded, reviewed against the GovEngine host-neutral boundary, and ready for normal Signposter validation/report/PR/integration flow.
Validation: python -m pytest tests/test_execution_gate.py tests/test_execution_supervision.py -q

Goal:
Complete this narrow task: Implement optional constrained local runner only if readiness passes.

Target command:
```bash
python -m pytest tests/ -q
```

Expected output:
```text
Signposter <Feature>

Status:
ready / blocked / pending / completed

Notes:
No GitHub mutation was performed.
No OpenClaw execution was performed.
```

Scope:
* Do this exact thing.
* Do not do unrelated refactors.
* Do not mutate GitHub unless explicitly required and guarded by --apply.
* Do not close issues unless this task is specifically about issue close apply.
* Do not run OpenClaw unless this task is specifically about execution.
* Keep output compact and deterministic.

Dependencies:
* GE-031

Dependency metadata:
* key: GE-031
  github issue: assigned during guarded seed apply
  status: pending

Rules:
1. If a required precondition is missing, block safely.
2. If status is blocked, do not show misleading ready/apply wording.
3. If a mutation step fails, stop and report partial state clearly.
4. Never continue to later mutations after an earlier critical mutation failed.
5. Never print secrets or tokens.

Implementation guidance:
* Prefer modifying existing Signposter modules where practical.
* Add a new module only if the feature is clearly separate.
* Keep helper functions small.
* Keep CLI output human-readable and deterministic.
* Capture stderr/stdout for failed subprocesses when subprocesses are used.

Tests:
* Add or update targeted tests for the changed surface.
* Cover happy path / ready path.
* Cover blocked path for missing or unsafe preconditions.
* Cover safety notes in output.

Acceptance:
* GE-032: concrete scope is delivered with bounded evidence.
* GE-032: host-neutral GovEngine/SCLite boundary is preserved.
* GE-032: dry-run/default-safe behavior remains unchanged unless explicitly covered by tests.
* GE-032: validation command succeeds: python -m pytest tests/test_execution_gate.py tests/test_execution_supervision.py -q
* GE-032: no package publication, release, tag, upstream mutation, or secret change occurs.
* ruff check . passes.
* python -m pytest tests/ -q passes.
* No unintended GitHub mutations.
* No unrelated files changed.
* Existing flows unchanged.

Stop conditions:
* targeted pytest fails
* full pytest fails
* public truth validation fails
* CI fails
* GitHub mutation is requested without --apply
* execution backend is requested without explicit execution permission
* PR body contains auto-close keywords
* merge or integration plan is not ready
* live subprocess execution would be enabled before prerequisites are complete

Report back:
* whether a code bug was found
* chosen CLI shape
* files changed
* tests added/updated
* sample ready output
* sample blocked output
* ruff/pytest result
