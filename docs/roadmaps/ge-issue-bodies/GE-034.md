Task: GE-034 — Design inspect-only operator admission workflow

Task contract: GE-TASK-CONTRACT v1
Contract path: `docs/roadmaps/GE-TASK-CONTRACT.md`

Purpose:
- Expose operator visibility only after runtime admission records are stable.

Concrete scope:
- define a script or CLI workflow that reads an admission record and prints compact allowed/blocked/blockers/next-actions output without executing live work.

Non-goals:
- no broad rewrite, no live backend enablement, no PKI/KMS ownership, no Ravenclaw/OpenClaw/MCP/A2A core dependency, no package publish.

Dependencies:
- GE-013 (#13)
- GE-022 (#22)

Route / gate / risk / model:
- route: role:worker
- gate: gate:ci
- risk: medium
- expected model tier: standard coding worker; use stronger reasoning for governance ambiguity

Mutation boundary:
- dry-run first; guarded Signposter apply only; no upstream mutation; no package publish; no tags
- No upstream mutation, package publish, release, tag, secret change, or manual issue closure.
- PR text must avoid auto-close keywords; integration owns issue closure.

Acceptance criteria:
- GE-034: concrete scope is delivered with bounded evidence.
- GE-034: host-neutral GovEngine/SCLite boundary is preserved.
- GE-034: dry-run/default-safe behavior remains unchanged unless explicitly covered by tests.
- GE-034: validation command succeeds: python scripts/validate_public_truth.py && python -m pytest tests/test_admission_contracts.py -q
- GE-034: no package publication, release, tag, upstream mutation, or secret change occurs.
- Standard contract validation and reporting evidence are complete.

Validation:
- `python scripts/validate_public_truth.py && python -m pytest tests/test_admission_contracts.py -q`
- `ruff check .`
- `python -m pytest tests/ -q`
- Run public truth / alpha validators when docs, public status, package truth, or surfaces are touched.

Done criteria:
- scoped implementation or audit artifact is complete, bounded, reviewed against the GovEngine host-neutral boundary, and ready for normal Signposter validation/report/PR/integration flow.
- Worker summary, PR, CI, review, merge, integration, cleanup, and planner advance complete.

Task-specific stop conditions:
- Stop if task validation, full pytest, public truth, CI, review, merge, or integration fails.
- Stop if the work would enable live execution before prerequisites or cross host/SCLite ownership boundaries.

Return behavior:
- After completion, follow planner advance/next and return to the mainline or required side-task selected by Signposter.
